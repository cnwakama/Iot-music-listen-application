{
  "name": "app",
  "version": "1.0.0",
  "description": "This is where the server will be placed holding information of about the sound of the note through time series database",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "influx": "^5.5.1",
    "socket.io-client": "^2.3.0"
  }
}
